{
  "name": "axiomify-workspace",
  "description": "Zero-compromise Node.js framework with Radix routing, Zod-native validation, and a modular plugin ecosystem for building high-performance, type-safe APIs.",
  "private": true,
  "version": "4.0.0",
  "engines": {
    "node": ">=18.0.0"
  },
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "npm run build:core && npm run build:adapters && npm run build:plugins && npm run build:cli",
    "build:core": "npm run build -w @axiomify/core",
    "build:adapters": "npm run build -w @axiomify/native -w @axiomify/express -w @axiomify/http -w @axiomify/fastify -w @axiomify/hapi",
    "build:plugins": "npm run build -w @axiomify/auth -w @axiomify/cors -w @axiomify/fingerprint -w @axiomify/graphql -w @axiomify/helmet -w @axiomify/logger -w @axiomify/metrics -w @axiomify/openapi -w @axiomify/rate-limit -w @axiomify/security -w @axiomify/static -w @axiomify/upload -w @axiomify/ws",
    "build:cli": "npm run build -w @axiomify/cli",
    "build:all": "npm run build --workspaces --if-present",
    "clean": "rm -rf packages/*/dist && rm -rf packages/*/.tsconfig.tsbuildinfo",
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@changesets/cli": "^2.30.0",
    "@types/node": "^25.5.2",
    "@vitest/coverage-v8": "^3.2.4",
    "fastify-plugin": "^5.1.0",
    "tsup": "^8.5.1",
    "typescript": "^6.0.2",
    "vitest": "^3.2.4"
  },
  "keywords": [
    "nodejs",
    "framework",
    "web-framework",
    "backend",
    "api",
    "http",
    "server",
    "fastify-alternative",
    "express-alternative",
    "nestjs-alternative",
    "high-performance",
    "radix-router",
    "routing",
    "zod",
    "validation",
    "type-safe",
    "typescript",
    "schema-first",
    "plugin-system",
    "microservices",
    "rest-api"
  ]
}
