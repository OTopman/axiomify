export * from './generator';
export * from './emitter';
export * from './registry';

// Built-in targets will be registered dynamically by the CLI
// to avoid loading all languages if only one is requested.
